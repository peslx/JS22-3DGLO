// Инициализация сборки Webpack
npm init [-y]

// Формирование зависимостей по шаблону 'package.json'
npm install webpack webpack-cli --save-dev

// Формирование зависимостей по шаблону 'package.json'
npm i

// Собрать проект
npm run build

// Отслеживание изменений в реальном времени
npm run watch

// Live-server
npm run live